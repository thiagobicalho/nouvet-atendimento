---
updated: 2026-09-01T16:23
---

- (decision by thiago) Build iniciado a partir da party mode: skill de referencia n8n grounded em exports reais de workflows n8n de projetos anteriores da Btech/Unique, nao MCP nem CLI (nao ha n8n MCP/CLI de instancia viva configurado para o projeto Nouvet; MCP oficial de docs do n8n foi configurado separadamente em escopo global, fora desta skill).
- (assumption) Achado critico durante o build: o primeiro exemplo fornecido (reference/modelo-n8n/clinica/, 9 arquivos) hardcoda toda a personalizacao (tom, regras) direto no systemMessage do agente LangChain (~27KB de texto), NAO lendo de config externa - isso NAO satisfaz o FR-34/35 do PRD do Nouvet (config editavel sem redeploy). Thiago entao apontou um segundo projeto mais maduro, reference/modelo-n8n/secretariav3-completo/ (16 arquivos), que resolve isso corretamente: tabela Postgres 'secretaria_config' (singleton, id=1) + 'secretaria_profissionais', populadas por um workflow de provisionamento via webhook (00 - Configuracoes.json), lidas a cada turno de conversa por um node Postgres 'Buscar Config' + um node Set 'Info' que reshapeia os campos, e o systemMessage do agente referencia os campos via expressions tipo {{ .item.json.nome_secretaria }} - ou seja, config de verdade fora do fluxo, editavel via UPDATE no banco, sem redeploy. Esse e o padrao que a skill vai documentar como referencia principal para a arquitetura do Nouvet; o padrao hardcoded do primeiro exemplo fica documentado como contraste/gotcha (o que NAO fazer).
- (assumption by thiago) Correcao da entrada anterior (comeu caracteres por causa de $(...) no shell): o node Postgres chamado "Buscar Config" roda `SELECT c.*, (SELECT json_agg(p.*) FROM secretaria_profissionais p WHERE p.ativo = true) as profissionais FROM secretaria_config c WHERE c.id = 1`; um node Set chamado "Info" reshapeia esses campos; e o systemMessage do agente LangChain referencia os campos via expressions n8n no formato: chave $ parenteses aspas simples Info aspas simples fecha parenteses ponto item ponto json ponto nome_secretaria (a sintaxe exata de expressao n8n de acesso a outro node por nome, aplicada a cada campo de config). Fica registrado por extenso aqui para nao repetir o problema de shell-escaping.
- (note) Achado adicional relevante para o FR-41 do PRD do Nouvet (alerta de emergencia a todos os profissionais envolvidos, configuravel): o exemplo real ja tem exatamente esse padrao pronto - workflow 05.1 Escalar Humano Multi, onde o campo id_conversa_alerta na config vira um array em vez de um ID unico, um node splitOut quebra a lista, e o envio de alerta roda uma vez por destinatario. Documentado em references/agente-e-subfluxos.md como precedente direto.
- (event) SKILL.md e as duas references escritas: config-postgres.md (schema completo das tabelas operacionais e de configuracao, provisionamento via webhook, leitura em runtime via node Buscar Config mais node Info) e agente-e-subfluxos.md (padrao de debounce/lock na entrada, estrutura do agente principal, sub-workflows de agenda como ferramentas, escalar humano single vs multi, satelites fora de escopo do Nouvet como Asaas/Twilio/leads/Evolution). Nao foram criados scripts - toda a skill e referencia estatica de leitura, nenhuma sequencia deterministica identificada que justificasse um script proprio.
- (note) Lint gate rodado: quick_validate.py OK (sem erros), scan-scripts.py OK (sem scripts/, esperado para skill de referencia estatica), scan-path-standards.py sinalizou .memlog.md na raiz como high severity - mesmo falso positivo ja documentado na skill rd-station-api: o proprio build-process.md do builder manda escrever o memlog exatamente em target-skill-path barra ponto memlog ponto md. Nao movido para references/. Token count do SKILL.md: 1389 (dentro do orcamento de 2000 desejado).
- (event) analyze: grade fair, 0 critical / 3 high / 4 medium / 0 low, report .analysis/2026-09-01-1609/skill-analysis-report.html. Corrigidos nesta sessao: leanness-1 (exemplo de arquivo quebrado no Resolution rules, secao mantida por decisao de arquitetura) e architecture-1 (path sem prefixo project-root). Enhancement-1 a 5 ficam em aberto, levados para avaliacao em party mode junto com a decisao de manter ou nao os dois pacotes de referencia (clinica e secretariav3-completo).
- (decision by thiago) Aplicadas as decisoes da party mode sobre o relatorio de analyze: clinica rebaixado a contraexemplo citado uma vez (nao mais documentado em paralelo com peso igual a secretariav3-completo); adicionados os padroes que faltavam - agente proativo/agendado (lembretes/follow-up via cron ou mudanca de etapa), multimodal (audio de entrada, voz de saida, arquivo como ferramenta via 02), no ThinkTool no agente principal; ajustado o item Evolution para nao afirmar como fato que o padrao de pacing de mensagens se aplica a RD Conversas (fica como pergunta em aberto a checar na skill rd-station-api). Assistente Interno (08) NAO excluido como Asaas/Twilio/leads - por pedido explicito de Thiago, fica documentado como padrao pesquisavel para fase futura do projeto (bate com o Agente Interno para Gestores da visao de longo prazo do PRD), nao como Piloto atual.
- (note) Re-rodado o lint apos as edicoes: quick_validate OK. scan-path-standards mostrou 5 findings novos, todos dentro de .analysis/2026-09-01-1609/ (paths absolutos no campo standards do findings.json, exigidos pelo proprio schema do scan-orchestration.md, e uma citacao textual do bug ja documentado dentro do relatorio renderizado) - mesmo padrao de falso positivo do .memlog.md na raiz, aplicado agora a pasta .analysis/ que tambem e um artefato sancionado do proprio builder, nao conteudo real da skill. Nao editado findings.json nem o relatorio (nunca editar a mao por instrucao do scan-orchestration.md).
