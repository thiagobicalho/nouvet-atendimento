[pytest]
# Torna o pacote `importador/` importável pelos testes independente de como pytest é
# invocado (`pytest -q` vs `python -m pytest`) -- sem isto, `from importador import
# ...` falha porque pytest não adiciona o diretório corrente ao sys.path por padrão.
pythonpath = .
